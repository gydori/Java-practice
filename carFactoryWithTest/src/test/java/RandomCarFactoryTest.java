/*
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.junit.jupiter.api.Assertions.assertTrue;

public class RandomCarFactoryTest {
    private final CarFactory carFactory = new RandomCarFactory();

    @Test
    public void createCar() {
        Car car = carFactory.createCar("robi", 1230, 100, Color.RED);
        assertNotNull(car);
        assertTrue(car instanceof Skoda || car instanceof Lada || car instanceof Tesla);
    }

}*/
